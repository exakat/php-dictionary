# Parameter
A parameter is a variable in a method definition. They have a name, and may have a default value and type. They may also be passed by reference, or by value, which is the default way.
```php
<?php

function foo($parameter) {}

$argument = 1;
foo($argument);

?>
```

Related : [Argument](Argument), [Static Expression](Static Expression), [Comma](Comma), [Constant Scalar Expression](Constant Scalar Expression), [Default Value](Default Value), [Inherited Variable](Inherited Variable), [New In Initializers](New In Initializers), [Optional Parameter](Optional Parameter)
