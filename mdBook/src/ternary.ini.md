# Ternary Operator
The expression ``(expr1) ? (expr2) : (expr3)`` evaluates to ``expr2`` if ``expr1`` evaluates to true, and ``expr3`` if ``expr1`` evaluates to false.
```php
<?php

    $action = (empty($_POST['action'])) ? 'default' : $_POST['action'];

?>
```

## See Also

+ [Ternary Operator in PHP | How to use the PHP Ternary Operator](https://www.codementor.io/@sayantinideb/ternary-operator-in-php-how-to-use-the-php-ternary-operator-x0ubd3po6)

Related : [Short Ternary Operator](Short Ternary Operator), [Short Ternary Operator](Short Ternary Operator), [Coalesce Operator](Coalesce Operator), [If Then Else](If Then Else), [Binary Operator](Binary Operator), [Colon](Colon), [Operand](Operand), [Unary Operator](Unary Operator), [Nesting](Nesting)
